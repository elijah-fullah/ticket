import 'package:flutter/material.dart';
import '../../../widgets/universal/heading_1.dart';

class OneWebSetting extends StatelessWidget {
  const OneWebSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Heading1(text: 'Setting'),
    );
  }
}
