import 'package:flutter/material.dart';
import '../../../widgets/universal/heading_1.dart';

class TwoWebSetting extends StatelessWidget {
  const TwoWebSetting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Heading1(text: 'Setting'),
    );
  }
}
