import 'package:flutter/material.dart';
import '../../../widgets/universal/heading_1.dart';

class SuperMobileSettings extends StatelessWidget {
  const SuperMobileSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Heading1(text: 'Mobile Settings'),
    );
  }
}
