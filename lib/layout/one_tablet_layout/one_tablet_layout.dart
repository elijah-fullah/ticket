import 'package:flutter/material.dart';

class OneTabletLayout extends StatelessWidget {
  const OneTabletLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('One Tablet Layout'),
      ),
    );
  }
}
