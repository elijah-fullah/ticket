import 'package:flutter/material.dart';

class ThreeMobileLayout extends StatelessWidget {
  const ThreeMobileLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Three Mobile Layout'),
      ),
    );
  }
}
