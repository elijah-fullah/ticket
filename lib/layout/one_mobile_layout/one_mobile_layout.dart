import 'package:flutter/material.dart';

class OneMobileLayout extends StatelessWidget {
  const OneMobileLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('One Mobile Layout'),
      ),
    );
  }
}
