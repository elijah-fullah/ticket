import 'package:flutter/material.dart';

class ThreeTabletLayout extends StatelessWidget {
  const ThreeTabletLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Three Tablet Layout'),
      ),
    );
  }
}
