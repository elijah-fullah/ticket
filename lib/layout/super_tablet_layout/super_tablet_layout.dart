import 'package:flutter/material.dart';

class SuperTabletLayout extends StatelessWidget {
  const SuperTabletLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Super Tablet Layout'),
      ),
    );
  }
}
